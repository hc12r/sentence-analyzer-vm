apiVersion: apps/v1
kind: Deployment
metadata:
  name: sentence-analyzer-vm
  namespace: sentence-analyzer-vm
  labels:
    app: sentence-analyzer-vm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sentence-analyzer-vm
  template:
    metadata:
      labels:
        app: sentence-analyzer-vm
    spec:
      containers:
      - name: sentence-analyzer-vm
        image: ${DOCKER_REGISTRY}/sentence-analyzer-vm:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: "0.5"
            memory: "512Mi"
          requests:
            cpu: "0.2"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5